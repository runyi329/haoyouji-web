# 牙伴顾客列表页 — P1 / P2 优化建议清单

> 背景：P0（顶部统计条、强化搜索+防抖、快捷筛选 Chips、排序、无限滚动分页、紧凑卡片、头像联动）已全部完成并上线。
> 本文档列出后续 P1（强烈建议）和 P2（锦上添花）的优化项，供逐项确认是否实施。面向万级顾客规模设计。

---

## P1 — 强烈建议（直接影响日常使用效率）

### P1-1 字母/拼音索引条（A–Z 快速定位）
在列表右侧加一条竖向 A–Z 索引条，按姓名拼音首字母分组并支持点击跳转。万级顾客靠滚动找人不现实，这是通讯录类列表的标配。
- 实现要点：后端按拼音首字母排序并返回 `initial` 字段；前端右侧吸附索引条 + 分组吸顶标题。
- 价值：高。找特定顾客从"翻页"变成"一点直达"。

### P1-2 多条件高级筛选（抽屉式）
当前快捷筛选是单选 Chips，P1 增加一个"筛选"抽屉，支持组合条件：
- 性别、年龄段、来源渠道、负责医生/咨询师、有无手机号、标签。
- 多个条件可叠加，顶部显示"已选 N 个条件"，可一键清除。
- 价值：高。诊所运营经常要按"某医生名下 + 近30天未回访"这类组合捞人。

### P1-3 列表项右滑快捷操作
卡片左滑/右滑露出快捷按钮：拨打电话、发起随访、加标签。
- 价值：中高。减少"进详情→再操作"的层级，符合移动端习惯。

### P1-4 空状态与异常态完善
- 无数据时显示友好空状态插画 + "去新建顾客"按钮。
- 搜索/筛选无结果时区分文案（"没有符合条件的顾客"vs"还没有顾客"）。
- 接口失败时显示重试按钮，而非一直骨架屏。
- 价值：中高。提升健壮性，避免再出现"白屏不知所措"。

### P1-5 搜索历史与搜索建议
- 记录最近搜索关键词（本地），点击即可快速复用。
- 输入时下拉联想匹配的顾客姓名。
- 价值：中。高频搜索场景体验更顺。

---

## P2 — 锦上添花（有余力再做）

### P2-1 批量管理模式
长按或点"管理"进入多选模式，支持批量打标签、批量发起随访、批量导出。
- 价值：中。运营批量操作时有用，但日常使用频率较低。

### P2-2 顾客数据导出
按当前筛选条件导出 Excel/CSV（姓名、手机、来源、最近就诊等）。
- 注意：涉及顾客隐私，需限定管理员权限 + 操作留痕。
- 价值：中。

### P2-3 列表视图切换（紧凑/详细）
提供"紧凑卡片 / 详细卡片"两种密度切换，详细模式多展示最近就诊、负责医生等。
- 价值：中低。P0 已做紧凑卡片，此为偏好补充。

### P2-4 顾客标签体系可视化
为顾客打自定义彩色标签（如"VIP""种植意向""欠费"），列表卡片彩色小标呈现，并可按标签筛选。
- 价值：中。需配套标签管理后台，工作量略大。

### P2-5 统计条点击联动筛选
点"今日新增"统计数字，列表自动切到"今日新增"筛选；点"本月新增"同理。
- 价值：中低。小交互，提升数据与列表的连贯性。

### P2-6 滚动性能与虚拟列表
万级数据下，无限滚动累积过多 DOM 时引入虚拟滚动（react-virtuoso 等）只渲染可视区。
- 价值：中。当单次加载顾客数极大、出现卡顿时再做。

### P2-7 下拉刷新
列表顶部下拉手势触发刷新（重新拉取第一页 + 统计）。
- 价值：中低。移动端常见，但当前切筛选/搜索已会自动刷新。

---

## 实施优先级建议

| 优先级 | 建议项 | 预估价值 | 预估工作量 |
|--------|--------|----------|------------|
| 1 | P1-4 空状态/异常态 | 高 | 小 |
| 2 | P1-1 拼音索引条 | 高 | 中 |
| 3 | P1-2 高级筛选抽屉 | 高 | 中 |
| 4 | P1-3 右滑快捷操作 | 中高 | 中 |
| 5 | P2-5 统计联动筛选 | 中低 | 小 |
| 6 | P1-5 搜索历史 | 中 | 小 |
| 7 | 其余 P2 | 中—低 | 视项而定 |

> 建议先做"价值高 / 工作量小"的组合：**P1-4（空状态）+ P2-5（统计联动）**可以很快补齐，体验提升明显；随后推进 **P1-1 拼音索引 + P1-2 高级筛选**这两个面向万级规模的核心能力。
