以下是基于视频内容，对“e看牙”App中日程功能的详细拆解和描述：

### 1. 日程列表页面的布局和内容
*   **顶部导航栏**：左侧为返回箭头 `<`，中间标题为 **“预约/日程”**（带有下拉箭头，可能用于切换不同类型的日程），旁边有 **“随访”** 标签。右侧有一个 **筛选/视图设置** 图标。
*   **日期选择区域（折叠状态）**：横向排列的一周日期（周一至周日），显示公历日期（如08, 09, 10, 11）和农历/月份（如廿八, 廿九, 三十, 10月）。当前选中的日期（如“今 11日”）会用蓝色圆形高亮显示。下方有一个向下的双箭头 `˅` 用于展开日历。
*   **悬浮按钮**：页面右下角始终悬浮一个白底黑字的 **“+”** 按钮，用于新建日程/预约。
*   **列表内容展示（列表视图下）**：
    *   以卡片形式纵向排列每天的预约。
    *   每个卡片包含：患者姓名（如“王瑛”）、右上角的**状态标签**、**时间**（如 17:30-18:00）、**医生**（如 郑莹）、**项目**（如 复诊）、**备注**（如 宁缺访邀约 IMP定检）。

### 2. 日程详情页面
点击某个具体日程（如“王瑛”的预约）后进入“预约详情”页，布局如下：
*   **顶部**：左侧返回键，中间标题“预约详情”，右侧有“+”号。下方醒目位置展示当前状态标签（如绿底白字的 **“已离开”**）。
*   **患者基本信息卡片**：包含患者头像、姓名（王瑛）、年龄（45岁）、性别图标、患者编号（007368）、所属门店。点击该卡片可跳转至“患者详情”页。
*   **快捷操作区（四个图标按钮）**：**收费记录**、**病历记录**、**影像记录**、**发短信**。
*   **详细信息列表（键值对展示）**：
    *   **日期**：2026年06月02日 星期二 17:30-18:00
    *   **诊所**：上海恒恩口腔门诊部
    *   **医生**：郑莹
    *   **咨询师**：未指定咨询师
    *   **助理**：未指定助理
    *   **项目**：复诊
    *   **诊室**：未指定诊室
    *   **科室**：空白
    *   **预约来源**：未指定预约来源
    *   **备注**：宁缺访邀约 IMP定检
*   **底部操作区**：包含 **“查看变更记录”** 和 **“再次预约”** 按钮。

### 3. 创建/编辑日程的表单字段
*注：视频中并未直接展示点击“+”号后弹出的新建/编辑表单页面。*
但根据“预约详情”页展示的内容，可以反推创建/编辑日程时包含以下核心字段：
**时间/日期、诊所门店、指定医生、指定咨询师、指定助理、诊疗项目、指定诊室、科室、预约来源、备注信息**。

### 4. 各种筛选和切换功能
点击列表右上角的设置图标，会从右侧滑出 **“视图”** 抽屉菜单，包含强大的筛选和切换功能：
*   **视图切换**：提供三种UI模式选择：**日视图**（时间轴）、**周视图**（视频中未选中）、**列表视图**（卡片列表）。
*   **按医生筛选**：可选择“全部医生”，或横向滑动选择特定医生（如 郑莹、易家宝、李华超等）。
*   **按诊室筛选**：可选择“全部诊室”，或特定诊室（如 1号诊室、儿牙诊室、VIP诊室2 等）。
*   **按预约状态筛选**：提供多选标签，包括：全部状态、已预约、已确认、咨询中、已挂号、治疗中、治疗完成、已结账、已离开、失约。
*   **底部操作**：**“重置”** 和 **“确认”** 按钮。

### 5. 时间轴/日历的展示方式
*   **时间轴展示（日视图）**：
    *   左侧为纵向的时间刻度，从 08:00 开始，每半小时一个刻度（08:30, 09:00...）。
    *   右侧为日程区块（Block）。不同医生的预约以不同颜色的色块并排或交错显示在对应的时间段内。色块内显示患者姓名、项目简称（如“复诊”）、医生姓名及简短备注。
*   **日历展示（展开状态）**：
    *   点击日期栏下方的箭头，周历会向下展开为 **整月日历（月视图）**。
    *   日历上方有年份和月份切换按钮（如 `< 06 >`）。
    *   日历网格中，有日程安排的日期下方会显示**小圆点**作为提示。选中的日期会用蓝色圆形高亮。

### 6. 预约状态的颜色和标签
App通过颜色和明确的文字标签来区分预约状态：
*   **列表视图中的状态标签**（位于卡片右上角）：
    *   **待计划**：黄色文字标签。
    *   **已离开**：绿色文字标签（在详情页中显示为绿底白字）。
    *   **失约**：灰色文字标签。
*   **时间轴（日视图）中的色块颜色**：
    虽然视频中未明确标出每个颜色对应的具体状态，但时间轴上的预约块使用了丰富的颜色进行区分（可能代表不同状态、不同医生或不同项目类型），视频中出现的色块颜色包括：
    *   **灰色**（如患者殷伟民，备注显示“取消”，通常代表已取消或失约）。
    *   **绿色**（如患者赵玲、喻永丽，可能代表已确认或已完成）。
    *   **橙色/黄色**（如患者王平华、徐进）。
    *   **紫色/粉色**（如患者王雯婷、胡星民）。
    *   **蓝色**（在筛选菜单中，“已预约”状态的默认选中颜色为蓝色）。