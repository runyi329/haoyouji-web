# 牙伴平台：耗材库存模块重设计方案

基于对参考网站截图的分析，以及牙科行业耗材管理的特殊诉求（批号效期管理严、领用频次高、分类繁杂、椅位/医生维度的成本核算），我从前端 UI 设计与架构师的视角，为你梳理了一套「专业且便于操作」的库存模块重设计方案。

## 一、 参考竞品分析与体验痛点

通过你提供的截图（图1-图7），可以看出参考网站的库存模块具备以下特点：
- **优点**：支持左侧二级科室分类（技工、牙体牙髓、正畸等），右侧商品列表支持多选批量出入库；详情页有批次与变动记录明细。
- **痛点（也是我们可以超越的地方）**：
  1. **批量操作效率低**：在手机端，左侧分类菜单过窄且滑动容易误触，右侧勾选后只能看到「已选中X个」，无法在当前页直接修改每个物品的出入库数量，必须点「下一步」才能填数，导致流程割裂。
  2. **扫码入库入口深**：扫码是耗材入库最高频的操作，参考站把它藏在了「物品详情 -> 右上角加号」里，路径太深。
  3. **缺乏效期预警直观感知**：牙科耗材（如树脂、麻药、粘接剂）效期极短，参考站的列表页和详情页都没有直观的近效期颜色高亮预警。

## 二、 牙伴库存模块：全新信息架构

为了让诊所护士长和库管员操作更爽，我们把库存模块分为 **4 个核心主页**：

1. **工作台（Dashboard）**：库存概览、近效期/低库存预警提醒、快捷扫码入口。
2. **库存一览（Inventory）**：以分类树+列表的形式展示当前所有物品的实时库存。
3. **入库单（Inbound）**：包含采购入库、退货入库、盘盈入库。
4. **出库单（Outbound）**：包含领用出库（关联到医生/椅位）、报损出库、退货出库。

## 三、 关键交互重设计（移动端优先）

### 1. 极速「领用出库」交互（超越参考站）
牙科日常最高频的操作是护士给医生领用耗材。
- **放弃传统的「先勾选、再下一步填数量」**。
- **采用「购物车模式」**：
  - 左侧是科室分类（宽度占屏幕 25%，支持平滑滚动）。
  - 右侧是物品列表，每个物品卡片上直接提供 `- 0 +` 的步进器。
  - 护士点 `+` 号直接将物品加入底部的「出库清单（悬浮条）」，数量实时更新。
  - 点击底部悬浮条直接弹出明细确认并选择「领用医生/椅位」，一键提交。

### 2. 「扫码入库」前置
- 在库存模块的任何列表页，**右上角常驻一个显眼的「扫码」Icon**。
- 护士收到供应商包裹后，直接点扫码，扫描包装上的 UDI 码或条码。
- 系统自动带出物品信息，直接弹出弹窗要求输入：**批号、效期、数量**。确认即入库。

### 3. 批号与效期的「红绿灯」视觉管理
- 在「库存一览」列表和「物品详情」页：
  - 正常效期：灰色/黑色文本。
  - **近效期（如不足 3 个月）**：橙色标签高亮。
  - **已过期**：红色标签+删除线警告，并在出库时强制拦截拦截。

## 四、 界面布局与 UI 规范建议

遵循你一贯的「专业、统一、移动端优先」偏好：

- **色彩规范**：沿用全局的红/金/白/深色系。预警使用品牌红（而非突兀的系统红），正常操作按钮使用全局统一色。
- **容器布局**：避免屏幕大面积留白。列表卡片采用极简微边框或纯白底色+浅灰分割线，去除厚重的阴影。
- **表单设计**：出入库填写的输入框宽度与下方元素对齐，数字键盘自动唤起。批次信息（批号、生产日期、有效期）采用紧凑的 Grid 网格两列排布，一屏看完。

## 五、 底层数据模型（架构师视角）

为了支撑上述体验，后端数据库需要设计如下核心表结构（相比单表库存，必须引入**批次表**）：

1. `yaban_material`（物品基础信息表）：名称、规格、单位、分类、警戒库存值。
2. `yaban_stock_batch`（库存批次表，**核心**）：记录每个物品在每家门店的每个批号、效期及当前剩余数量。
3. `yaban_inventory_log`（出入库流水表）：记录时间、操作类型（采购/领用/盘点）、数量变动、关联批次、操作人、领用人/椅位。

---

**总结**：这套方案舍弃了参考站繁琐的「多选 -> 下一步」流程，改用更适合高频操作的「购物车模式」，并将牙科刚需的「扫码」和「效期预警」提到了最高层级。

如果你认可这个设计方向，我们可以先从「物品基础信息」和「库存一览」的列表页开始开发落地。
