# 牙伴影像记录 · 存储与权限方案（折中设计）

> 目标：在腾讯云 COS（现有牙伴存储）基础上，建立真正的影像存储能力。既要控制存储成本（不能全量存原图），又不能因压缩损害 X 光 / CBCT 的诊断质量。

## 一、核心矛盾

现有 COS 上传（`cos-upload.ts`）对所有图片统一压成 **WebP、最长边 1200px、质量 85**。这套参数对生活照、收据足够，但对医学影像是**不可接受**的：

- X 光片、根尖片、全景片放大看细微骨纹理 / 龋坏边缘，1200px + 有损 WebP 会把诊断细节抹掉；
- CBCT、口扫 stl 是三维 / 专业格式，根本不能当普通图片压缩。

但若**全部存原图**，单张全景片动辄 5–20MB、CBCT 可达数百 MB，存储与流量成本会失控。

## 二、折中方案：分层存储（缩略图 + 高清原图 双轨）

核心思路：**一次上传，存两份**——一份小的供列表快速浏览，一份高保真的供诊断查看。按影像类型决定"高保真份"的压缩强度。

### 1. 两份文件

| 份 | 用途 | 处理方式 | 体积 |
|----|------|---------|------|
| 缩略图 thumb | 列表网格快速加载 | WebP，最长边 400px，质量 70 | 约 10–40KB |
| 高清原图 full | 点开大图、诊断、下载 | **按类型分级**（见下） | 视类型而定 |

列表只加载缩略图（省流量、秒开），用户点开某张时才按需加载高清原图。

### 2. 高清份按影像类型分级处理（关键）

| 影像类型 | 高清份策略 | 理由 |
|---------|-----------|------|
| X光片 / 根尖片 / 全景片 / CBCT 截图 / 内窥镜 | **完全保留原图，不压缩、不转码**（原格式原字节直传 COS） | 诊断级，任何有损压缩都可能影响判读，必须无损 |
| 口内照 / 面像照 / 美白对比照 | **轻度压缩**：最长边 2400px、WebP 质量 92（接近视觉无损） | 临床记录与沟通用，2400px 足够看清牙面细节，体积可控 |
| 文档图片（知情同意书等） | 最长边 2000px、质量 90（沿用现有 receipt 档） | 文字清晰可读即可 |
| CBCT 原始数据 / 口扫 stl / DICOM | **原文件直传，不做任何图像处理**，仅存档供下载 | 非普通图片，用专业格式，按文件存 |

> 实现上：诊断级类型走 `uploadFileToCOS`（原样直传）；照片类走改造后的 `uploadImageToCOS`（可传入"轻压缩档"参数）。两者都额外生成一份 400px 缩略图。

### 3. 上传时由"角色 + 分类"共同决定压缩档

上传者在选分类时，前端按分类自动套用上表策略，无需用户关心技术细节。管理员也可在高级选项里强制"原图无损存档"。

### 4. 成本控制兜底

- 单张原图超过阈值（如 30MB）时提示并允许，但记录用量；
- CBCT/stl 等超大文件给明确的大小上限（如 500MB）与进度条；
- 后续可加"冷数据归档"（很久未访问的原图转低频存储）进一步降本，本期先不做。

## 三、权限设计（逐步细分，本期先收口到创建者）

### 1. 角色与影像权限矩阵（目标态）

| 角色 | 查看影像 | 上传影像 | 删除影像 | 下载原图 |
|------|---------|---------|---------|---------|
| 网站创建者 / 超级管理员（super_admin） | ✅ | ✅ | ✅ | ✅ |
| 医生 / 前台（后续分配） | ✅ | ✅ | 限本人上传 | ✅ |
| 顾客本人（被授权时） | 仅授权范围 | ❌ | ❌ | 视策略 |
| 其他 | ❌ | ❌ | ❌ | ❌ |

### 2. 本期落地（你的要求）

- **上传 / 删除影像接口**：用 `super_admin` 角色门槛（即网站创建者，目前是 `jiang`），其他角色一律 FORBIDDEN。
- **查看影像**：登录用户即可查看（`protectedProcedure`），为后续"顾客被授权查看"预留；后续按需再收紧到顾客本人维度。
- 预留 `uploaderRole` / `uploaderId` 字段，后续分配医生、前台角色时直接放开门槛即可，无需改表。

> 复用项目已有的角色判定惯例：`ctx.user.role === 'super_admin'`（与 nbaOdds / wcOdds 里 adminProcedure 一致）。

## 四、数据库设计

新建 `yaban_customer_media` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| tenant_id | varchar | 租户 / 门店（沿用现有口径） |
| customer_id | bigint | 所属顾客 |
| category | varchar | 分类（面像照/X光片/口内照/CBCT/口扫stl/小牙片/文档图片/其他…） |
| stage | varchar | 治疗阶段（术前/术中/术后/复查/未分期），可空 |
| full_url | varchar | 高清/原图 COS URL |
| thumb_url | varchar | 缩略图 COS URL |
| file_name | varchar | 原始文件名 |
| file_size | int | 原图字节数 |
| mime | varchar | 文件 MIME |
| is_lossless | tinyint | 是否无损原图存档（诊断级=1） |
| note | varchar | 备注 |
| uploader_id | bigint | 上传者用户 id |
| uploader_role | varchar | 上传者角色（留痕，便于审计） |
| created_at | datetime | 创建时间（默认当前时间，修复"未来日期"问题） |

## 五、后端接口（tRPC，挂在 yabanCustomer 下）

| 接口 | 权限 | 说明 |
|------|------|------|
| `media.list` | 登录 | 按 customerId 列出影像，支持分类 / 阶段筛选，返回缩略图与原图 URL |
| `media.upload` | super_admin | 接收 base64/分类/阶段/备注，按策略生成缩略图+高清份并写表 |
| `media.update` | super_admin | 改备注 / 分类 / 阶段 |
| `media.delete` | super_admin | 删除记录并清理 COS 文件 |
| `media.stats` | 登录 | 各分类数量（给 Tab 角标用） |

## 六、本期实施范围（建议）

1. 建表 + 五个后端接口（含分层存储 + 权限收口到创建者）；
2. 改造 `cos-upload`：新增"轻压缩档/无损直传"与"生成缩略图"能力；
3. 前端影像页按之前的 UI 建议重做：3 列网格 + 日期/阶段分组、空分类不显示、上传动作菜单、大图缩放、备注浮层（对比/画笔标注列为后续 P2）。

确认这个折中方案（尤其是"诊断级无损 + 照片轻压缩 + 缩略图双轨"这条主线）后，我就按此实现。
