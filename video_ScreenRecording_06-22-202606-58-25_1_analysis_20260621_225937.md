在视频中，当用户点击“已选 1 个用户”的下拉框时，发生了以下情况：

1.  **下拉框是否展开**：下拉框**确实展开了**，在下方出现了一个带有“搜索用户名或 ID...”提示的搜索输入框。
2.  **异常行为**：当下拉框展开并显示搜索框时，系统的虚拟键盘会自动弹出。但紧接着，虚拟键盘会瞬间自动收起，同时展开的搜索框也会消失，页面恢复到点击前的状态。这个“下拉框展开 -> 键盘弹出 -> 键盘瞬间收起 -> 下拉框收起”的过程在视频中不断地循环重复，导致页面出现严重的闪烁现象。这属于明显的交互异常（Bug），使得用户根本无法稳定地使用搜索功能。