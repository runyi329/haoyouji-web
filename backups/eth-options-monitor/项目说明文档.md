# ETH 期权监控工具（eth-options-monitor）项目说明文档
> 本文档记录项目的完整技术信息、部署架构、环境变量、开发规范，供沙箱丢失后恢复使用。

---

## 一、项目基本信息

| 字段 | 内容 |
|---|---|
| 项目名称 | ETH 期权监控工具 |
| 项目代号 | eth-options-monitor |
| Manus 项目名 | `eth-options-monitor` |
| **Manus 项目 ID** | `ipCwyEQAy7sGhsuTwUZa63` |
| Manus 自动域名 | `ethoptmon-ipcwyeqa.manus.space` |
| 创建时间 | 2026年7月 |
| 项目定位 | 脉动网（jiangyuchen.cn）的附属子项目，用于实时监控 ETH Call 期权数据，辅助期权买入决策 |

---

## 二、访问地址

| 环境 | 地址 |
|---|---|
| 正式域名（自定义） | https://eth-options.jiangyuchen.cn |
| Manus 自动域名 | https://ethoptmon-ipcwyeqa.manus.space |
| 本地开发预览 | http://localhost:3000 |

---

## 三、托管与基础设施

### 托管平台
**全部托管在 Manus 云平台**，与脉动网的腾讯云服务器完全独立。

| 资源 | 位置 | 说明 |
|---|---|---|
| 服务器 | Manus 云（Cloudflare + Cloud Run） | Autoscale 模式，自动扩缩容 |
| 数据库 | Manus 内置 MySQL（TiDB） | 由 Manus 平台自动注入 DATABASE_URL |
| 存储 | Manus S3 对象存储 | 通过 storagePut/storageGet 操作 |
| CDN / SSL | Cloudflare | 自动配置，含自定义域名 SSL |

> ⚠️ 注意：此项目是独立 Manus WebDev 项目，GitHub 仓库（`backups/eth-options-monitor/`）只是代码备份，**不触发自动部署**。修改代码后需在 Manus 平台手动保存 Checkpoint 并点击 Publish 发布。

---

## 四、环境变量（Secrets）

以下为 Manus 平台自动注入的环境变量，恢复项目后会自动恢复，无需手动填写：

| 变量名 | 说明 |
|---|---|
| `DATABASE_URL` | MySQL 连接字符串，Manus 自动注入 |
| `JWT_SECRET` | Session cookie 签名密钥，Manus 自动注入 |
| `VITE_APP_ID` | Manus OAuth 应用 ID |
| `BUILT_IN_FORGE_API_KEY` | Manus 内置 API 密钥（服务端） |
| `VITE_FRONTEND_FORGE_API_KEY` | Manus 内置 API 密钥（前端） |

### 自定义环境变量（需手动配置）

| 变量名 | 值 | 说明 |
|---|---|---|
| `HAOYOUJI_SHARED_SECRET` | `mlm-bonus-shared-secret-2026` | 与脉动网 SSO 对接的 HMAC 共享密钥，两端必须一致 |

> ⚠️ 此密钥在脉动网（haoyouji-web）的 `server/routers.ts` 第 2795 行也有相同配置，两边必须保持一致。

---

## 五、自定义域名配置

### DNS 解析（腾讯云 DNSPod）

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| `eth-options` | CNAME | `ethoptmon-ipcwyeqa.manus.space` |

> 登录腾讯云控制台 → DNS 解析 → 选择 `jiangyuchen.cn` → 添加上述记录。

### Manus 后台绑定

在 Manus 项目 Settings → Domains 中添加 `eth-options.jiangyuchen.cn`，Cloudflare 会自动签发 SSL 证书（通常几分钟内生效）。

---

## 六、与脉动网主站的集成方式（SSO）

### 入口路径

脉动网首页积分商城 → 「母婴用品」分类（前端显示为「**期权链接**」）→ 跳转 `/eth-options` 路由 → `EthOptionsPage.tsx`（iframe SSO 嵌入）

> 注意：数据库里分类名仍为「母婴用品」，脉动网 `Home.tsx` 已做兼容处理：
> - 显示文字：`cat.name === '母婴用品'` 时强制显示为「期权链接」
> - 点击跳转：`cat.name === '期权链接' || cat.name === '母婴用品'` 都跳转 `/eth-options`

### SSO 流程（HMAC-SHA256 方案）

```
脉动网主站（haoyouji-web）
  ↓ 生成签名 URL
  GET https://eth-options.jiangyuchen.cn/api/auth/external-login
      ?uid=用户ID&name=用户名&ts=时间戳&sign=HMAC签名&redirect=/
  ↓
eth-options-monitor 后端（server/_core/oauth.ts）
  ↓ 验证签名（HAOYOUJI_SHARED_SECRET）
  ↓ 创建/更新用户记录
  ↓ 写入 session cookie
  ↓ 重定向到 /
```

### 脉动网主站相关代码位置

| 文件 | 说明 |
|---|---|
| `server/routers.ts` 第 2795 行附近 | `ethOptionsSsoLink` 过程，生成签名 URL，baseUrl 为 `https://eth-options.jiangyuchen.cn` |
| `client/src/pages/EthOptionsPage.tsx` | iframe 嵌入页面，包含 SSO 跳转逻辑 |
| `client/src/pages/Home.tsx` 第 1889 行附近 | 积分商城分类按钮点击逻辑 |

### CSP 配置（允许脉动网 iframe 嵌入）

`server/_core/index.ts` 中已配置：
```
frame-ancestors 'self' https://jiangyuchen.cn https://www.jiangyuchen.cn https://eth-options.jiangyuchen.cn
```

---

## 七、数据库表结构

### users 表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int PK | 自增主键 |
| openId | varchar(64) | Manus OAuth 或 SSO 用户 ID |
| name | text | 用户名 |
| loginMethod | varchar(64) | 登录方式（`haoyouji_sso` 或 OAuth） |
| role | enum(user/admin) | 角色 |
| createdAt / updatedAt / lastSignedIn | timestamp | 时间戳 |

### buy_records 表（持仓记录）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | varchar(64) PK | 客户端生成的唯一 ID |
| userId | int | 关联登录用户（可为空） |
| clientId | varchar(64) | 未登录时的匿名浏览器 ID |
| instrumentName | varchar(64) | 期权合约名，如 `ETH-25SEP26-3000-C` |
| strike | int | 行权价 |
| expiryLabel | varchar(32) | 到期日标签，如 `2026/09/25` |
| annualizedRate | double | 年化成本 |
| markPriceUsd | double | 买入时标记价（USD） |
| ethPriceAtBuy | double | 买入时 ETH 现价 |
| trueBreakeven | double | 真实平衡点 |
| note | text | 备注 |
| createdAt | timestamp | 创建时间 |

---

## 八、页面路由结构

| 路径 | 组件 | 说明 |
|---|---|---|
| `/` | `ProductDesign.tsx` | 默认首页（产品设计页） |
| `/legacy` | `Home.tsx` | 旧版期权监控主页（Deribit WebSocket 实时数据） |
| `/history` | `HistoryAnalysis.tsx` | 历史分析页 |
| `/product-design` | `ProductDesign.tsx` | 产品设计页 |
| `/annualized` | `AnnualizedChain.tsx` | 年化链页面 |
| `/iv-smile` | `IVSmile.tsx` | IV Smile 页面 |
| `/sso` | `SsoLogin.tsx` | SSO 降级入口（正常流程不经过此页） |

---

## 九、核心功能说明

### 数据来源
- **Deribit WebSocket API**（`wss://www.deribit.com/ws/api/v2`）
- 浏览器端直连，无需后端代理
- 监控 4 个到期日的 ETH Call 期权：25SEP26、25DEC26、26MAR27、25JUN27
- 筛选条件：行权价在 ETH 现价 85%~230% 范围内，且为 100 的整数倍

### 核心计算公式
- **年化成本** = 标记价(USD) / ETH现价 / 剩余年数
- **真实平衡点** = (行权价 + 权利金 - 分润% × ETH现价) / (1 - 分润%)
- **痛苦区** = ETH现价 至 真实平衡点

### ETH 现价获取
- CoinGecko API：`https://api.coingecko.com/api/v3/simple/price?ids=ethereum&vs_currencies=usd`
- 每 60 秒刷新一次

---

## 十、沙箱丢失后的恢复步骤

1. 在 Manus 平台直接打开 `eth-options-monitor` 项目（项目已存在，无需重建）
2. 沙箱路径：`/home/ubuntu/eth-options-monitor`
3. 如果是全新沙箱，需要用 `webdev_init_project` 绑定项目 ID `ipCwyEQAy7sGhsuTwUZa63`，才能保存 Checkpoint 和发布
4. 检查 Manus Secrets 面板，确认 `HAOYOUJI_SHARED_SECRET=mlm-bonus-shared-secret-2026` 已设置
5. 如需从 GitHub 备份恢复代码，备份位于 `backups/eth-options-monitor/`
6. 运行 `pnpm install && pnpm dev` 启动开发服务器

---

## 十一、开发规范

- 禁止使用 Emoji（包括代码注释、UI 文案、commit message）
- 数据库操作通过 Drizzle ORM，不直接写 SQL
- 修改代码后通过 Manus 平台 Checkpoint + Publish 发布，不通过 GitHub 触发部署
- 代码改动同步备份到 GitHub：`backups/eth-options-monitor/`（手动 rsync 或 cp）
- 遵循脉动网统一的技术栈版本（见 `AI-ONBOARDING.md` H-1.5 节）
