# 奖金制度研究平台（mlm-bonus-system）项目说明文档

> 本文档记录项目的完整技术信息、部署架构、环境变量、开发规范，供沙箱丢失后恢复使用。

---

## 一、项目基本信息

| 字段 | 内容 |
|---|---|
| 项目名称 | 奖金制度研究平台 |
| 项目代号 | mlm-bonus-system |
| Manus 项目 ID | `CHKNJmtWXcig3PadPxMzN3` |
| 创建时间 | 2026年7月 |
| 项目定位 | 脉动网（jiangyuchen.cn）的附属子项目，用于多层次直销奖金制度研究与模拟 |

---

## 二、访问地址

| 环境 | 地址 |
|---|---|
| 正式域名（自定义） | https://bonus.jiangyuchen.cn |
| Manus 自动域名 | https://mlmbonus-chknjmtw.manus.space |
| 本地开发预览 | http://localhost:3000 |

---

## 三、托管与基础设施

### 托管平台

**全部托管在 Manus 云平台**，与脉动网的腾讯云服务器完全独立。

| 资源 | 位置 | 说明 |
|---|---|---|
| 服务器 | Manus 云（Cloudflare + Cloud Run） | Autoscale 模式，自动扩缩容，min-instances=0 |
| 数据库 | TiDB Cloud（AWS us-east-1） | MySQL 兼容，Manus 托管 |
| 存储 | Manus S3 对象存储 | 通过 storagePut/storageGet 操作 |
| CDN / SSL | Cloudflare | 自动配置，含自定义域名 SSL |

### 数据库连接信息

```
DATABASE_URL=mysql://XTqR3P9v8tSgKnm.581e5149da8d:K1u39eviuz6nj9cNm6yj@gateway03.us-east-1.prod.aws.tidbcloud.com:4000/CHKNJmtWXcig3PadPxMzN3?ssl={"rejectUnauthorized":true}
```

> ⚠️ 注意：此连接字符串由 Manus 平台自动注入，不需要手动配置。恢复项目后在 Manus 后台 Database 面板可查看。

---

## 四、环境变量（Secrets）

以下为 Manus 平台自动注入的环境变量，恢复项目后会自动恢复，无需手动填写：

| 变量名 | 值 | 说明 |
|---|---|---|
| `VITE_APP_ID` | `CHKNJmtWXcig3PadPxMzN3` | Manus OAuth 应用 ID |
| `VITE_APP_TITLE` | `奖金制度研究平台` | 网站标题 |
| `JWT_SECRET` | `eAmtvHVLWShhDPNPBP5pru` | Session cookie 签名密钥 |
| `OWNER_OPEN_ID` | `JyRLJmQzLZdRutVgdLmjJs` | 项目 Owner 的 Manus ID |
| `OWNER_NAME` | `run` | 项目 Owner 名称 |
| `BUILT_IN_FORGE_API_KEY` | `Hqqhp9VbqDVLcTZBNFgUPo` | Manus 内置 API 密钥（服务端） |
| `VITE_FRONTEND_FORGE_API_KEY` | `UjeDpBCfU4qGUeEQaxoc2F` | Manus 内置 API 密钥（前端） |
| `VITE_ANALYTICS_WEBSITE_ID` | `0034d20d-6bf4-4cce-b41a-28dcded3a8df` | 统计分析 ID |

### 自定义环境变量（需手动配置）

| 变量名 | 值 | 说明 |
|---|---|---|
| `HAOYOUJI_SHARED_SECRET` | `mlm-bonus-shared-secret-2026` | 与脉动网 SSO 对接的 HMAC 共享密钥，两端必须一致 |

> ⚠️ 此密钥在脉动网（haoyouji-web）的 `server/routers.ts` 第 2795 行也有相同配置，两边必须保持一致。

---

## 五、自定义域名配置

### DNS 解析（腾讯云）

| 主机记录 | 记录类型 | 记录值 | 说明 |
|---|---|---|---|
| `bonus` | `CNAME` | `mlmbonus-chknjmtw.manus.space` | 指向 Manus 自动域名 |

### Manus 后台绑定

在 Manus 项目 Settings → Domains 已绑定：`bonus.jiangyuchen.cn`

---

## 六、与脉动网的 SSO 对接架构

### 整体流程

```
用户在脉动网登录
    ↓
点击积分商城"奖金制度"入口
    ↓
脉动网前端跳转到 /mlm-bonus 页面（内部路径）
    ↓
MlmBonusPage.tsx 调用后端 trpc.auth.mlmSsoLink
    ↓
脉动网后端用 HMAC-SHA256 生成签名链接
    签名内容：uid:name:ts
    密钥：HAOYOUJI_SHARED_SECRET
    有效期：5分钟（防重放攻击）
    ↓
iframe src 设为签名链接（https://bonus.jiangyuchen.cn/api/auth/external-login?uid=...&sign=...）
    ↓
奖金平台验证签名，自动登录用户
    ↓
用户无感知进入奖金平台
```

### 相关代码位置

**脉动网（haoyouji-web）：**
- 生成签名链接：`server/routers.ts` → `auth.mlmSsoLink` 过程（约第 2783 行）
- iframe 页面：`client/src/pages/MlmBonusPage.tsx`
- 积分商城入口：`client/src/pages/Home.tsx`（约第 1253 行）
- 路由注册：`client/src/App.tsx` → `/mlm-bonus` 路由

**奖金平台（mlm-bonus-system）：**
- 验证签名端点：`server/_core/oauth.ts` → `/api/auth/external-login`（约第 20-60 行）
- 允许 iframe 嵌入的响应头：`server/_core/index.ts`（`Content-Security-Policy: frame-ancestors`）

### 安全说明

- HMAC 密钥只存在于两个系统的**服务端**，前端不可见
- 时间戳有效期 5 分钟，防止链接被重放
- 奖金平台响应头设置 `frame-ancestors 'self' https://jiangyuchen.cn https://www.jiangyuchen.cn`，只允许脉动网域名嵌入

---

## 七、用户系统说明

### 两套独立用户系统

| 系统 | 用户数据存储位置 | 登录方式 |
|---|---|---|
| 脉动网 | 腾讯云服务器 MySQL | 账号密码 |
| 奖金平台 | Manus TiDB Cloud | SSO 自动登录（从脉动网跳入）|

### 设计决策：关闭独立注册

奖金平台定位为脉动网的附属工具，**不开放独立注册**，只允许从脉动网 SSO 跳入。

如需开放独立注册，修改 `client/src/pages/Login.tsx`，取消注释注册相关代码。

---

## 八、技术栈

| 层级 | 技术 |
|---|---|
| 前端框架 | React 19 + TypeScript |
| 样式 | Tailwind CSS 4 + shadcn/ui |
| 路由 | Wouter |
| 后端框架 | Express 4 + tRPC 11 |
| 数据库 ORM | Drizzle ORM |
| 构建工具 | Vite |
| 包管理 | pnpm |
| 测试 | Vitest |

---

## 九、沙箱丢失后的恢复步骤

### 第一步：找回 Manus 项目

1. 登录 Manus，在历史任务或项目列表中找到"奖金制度研究平台"
2. 项目 ID：`CHKNJmtWXcig3PadPxMzN3`
3. 线上网站和数据库不受影响，继续正常运行

### 第二步：恢复本地开发环境

新建沙箱后，告诉 Manus：
> "帮我从 GitHub `runyi329/haoyouji-web` 仓库的 `backups/mlm-bonus-system/` 目录恢复奖金制度研究平台（Manus 项目 ID：CHKNJmtWXcig3PadPxMzN3），继续开发"

### 第三步：确认环境变量

在 Manus 后台 Settings → Secrets 确认 `HAOYOUJI_SHARED_SECRET` 已设置为 `mlm-bonus-shared-secret-2026`。

### 第四步：确认域名绑定

在 Manus 后台 Settings → Domains 确认 `bonus.jiangyuchen.cn` 已绑定。

---

## 十、功能模块说明

| 模块 | 文件 | 说明 |
|---|---|---|
| 首页（公司列表） | `client/src/pages/Home.tsx` | 展示所有奖金制度公司，支持筛选 |
| 康宝莱详情 | `client/src/pages/HerbalifeDetail.tsx` | 完整引擎，8级会员体系 |
| 数研金控详情 | `client/src/pages/SyjkDetail.tsx` | 完整引擎，订阅制多层次 |
| 自定义方案 | `client/src/pages/CustomSchemeWizard.tsx` | 用户自建奖金方案 |
| 对比分析 | `client/src/pages/ComparePage.tsx` | 多公司横向对比 |
| 各公司模拟器 | `client/src/pages/simulators/` | 安利、玫琳凯、如新等 |
| 奖金计算引擎 | `server/mlm-bonus-engine.ts` | 核心计算逻辑 |
| 数研金控引擎 | `server/mlm-syjk-engine.ts` | 数研金控专用计算逻辑 |
| SSO 登录验证 | `server/_core/oauth.ts` | HMAC 签名验证端点 |

---

## 十一、代码备份说明

本代码备份位于 GitHub 仓库：
- 仓库：`runyi329/haoyouji-web`（私有）
- 路径：`backups/mlm-bonus-system/`
- 最后更新：2026年7月12日

> ⚠️ 此备份不含 `node_modules`、`.git`、`.manus-logs` 等目录，恢复后需执行 `pnpm install` 安装依赖。
