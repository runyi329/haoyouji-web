# 股权激励系统 - 数据库迁移指南

**版本：** 60b963b  
**创建时间：** 2026-02-13  
**迁移文件：** `migrations/add_equity_system.sql`

---

## 📋 迁移概述

本次迁移将创建3个新表，用于支持股权激励可视化系统：

1. **equity_investments** - 投资记录表（存储股东投资金额）
2. **equity_rules** - 规则配置表（存储股权计算规则）
3. **equity_contributions** - 贡献记录表（存储邀请、人脉等贡献）

---

## 🚀 迁移步骤

### 方法一：使用数据库管理工具（推荐）

#### 1. 登录数据库管理工具

根据您使用的工具选择：

**Navicat / DBeaver / phpMyAdmin：**
- 打开工具并连接到生产数据库
- 选择目标数据库（通常是 `haoyouji` 或类似名称）

**阿里云 / 腾讯云控制台：**
- 登录云服务商控制台
- 进入数据库管理 → SQL窗口

#### 2. 复制并执行SQL

**完整SQL语句：**

```sql
-- ==================== 股权激励系统数据库表结构 ====================
-- 创建时间: 2026-02-13
-- 版本: 60b963b

-- 1. 投资记录表
CREATE TABLE IF NOT EXISTS equity_investments (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL COMMENT '股东用户ID',
  investment_amount DECIMAL(15, 2) NOT NULL DEFAULT 0 COMMENT '投资金额（元）',
  investment_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '投资日期',
  notes TEXT COMMENT '备注',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user_id (user_id),
  INDEX idx_investment_date (investment_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股权投资记录表';

-- 2. 股权规则配置表
CREATE TABLE IF NOT EXISTS equity_rules (
  id INT PRIMARY KEY AUTO_INCREMENT,
  rule_key VARCHAR(100) NOT NULL UNIQUE COMMENT '规则键名',
  rule_value DECIMAL(10, 4) NOT NULL COMMENT '规则值',
  rule_description TEXT COMMENT '规则描述',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_rule_key (rule_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股权规则配置表';

-- 3. 贡献股份记录表
CREATE TABLE IF NOT EXISTS equity_contributions (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL COMMENT '用户ID',
  contribution_type VARCHAR(50) NOT NULL COMMENT '贡献类型: invite(邀请), referral_network(人脉贡献), other',
  contribution_value DECIMAL(10, 4) NOT NULL COMMENT '贡献值（股份百分比）',
  related_user_id INT COMMENT '关联用户ID（如被邀请人）',
  description TEXT COMMENT '贡献描述',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user_id (user_id),
  INDEX idx_contribution_type (contribution_type),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股权贡献记录表';

-- 插入默认规则配置
INSERT INTO equity_rules (rule_key, rule_value, rule_description) VALUES
('investment_pool_percentage', 33.3333, '投资股份池占总股本的百分比'),
('contribution_pool_percentage', 66.6667, '贡献股份池占总股本的百分比'),
('invite_per_user_percentage', 0.05, '每邀请1个用户获得的股份百分比'),
('referral_network_per_100_percentage', 0.02, '被邀请人每增加100人脉获得的股份百分比')
ON DUPLICATE KEY UPDATE 
  rule_value = VALUES(rule_value),
  rule_description = VALUES(rule_description);
```

#### 3. 执行并验证

**执行SQL后，检查结果：**

```sql
-- 验证表是否创建成功
SHOW TABLES LIKE 'equity%';

-- 应该看到3个表：
-- equity_contributions
-- equity_investments
-- equity_rules

-- 验证规则配置是否插入成功
SELECT * FROM equity_rules;

-- 应该看到4条规则记录
```

---

### 方法二：使用命令行（适合SSH连接）

如果您有服务器SSH访问权限：

```bash
# 1. 登录服务器
ssh user@your-server.com

# 2. 下载SQL文件（如果还没有）
cd /path/to/your/project
git pull origin main

# 3. 执行迁移
mysql -u your_username -p your_database_name < migrations/add_equity_system.sql

# 4. 验证
mysql -u your_username -p your_database_name -e "SHOW TABLES LIKE 'equity%';"
mysql -u your_username -p your_database_name -e "SELECT * FROM equity_rules;"
```

---

## ✅ 验证清单

迁移完成后，请确认以下内容：

- [ ] 3个表已成功创建（equity_investments, equity_rules, equity_contributions）
- [ ] equity_rules 表中有4条默认规则记录
- [ ] 外键约束已正确创建（user_id → users.id）
- [ ] 索引已创建（idx_user_id, idx_rule_key 等）

**验证SQL：**

```sql
-- 检查表结构
DESCRIBE equity_investments;
DESCRIBE equity_rules;
DESCRIBE equity_contributions;

-- 检查默认规则
SELECT rule_key, rule_value, rule_description FROM equity_rules;
```

**预期结果：**

| rule_key | rule_value | rule_description |
|----------|------------|------------------|
| investment_pool_percentage | 33.3333 | 投资股份池占总股本的百分比 |
| contribution_pool_percentage | 66.6667 | 贡献股份池占总股本的百分比 |
| invite_per_user_percentage | 0.0500 | 每邀请1个用户获得的股份百分比 |
| referral_network_per_100_percentage | 0.0200 | 被邀请人每增加100人脉获得的股份百分比 |

---

## 🎯 迁移后的下一步

### 1. 添加初始投资记录（管理员操作）

访问管理后台：`https://your-domain.com/admin/equity`

在"投资记录管理"中添加股东的投资金额：

- 选择股东（用户）
- 输入投资金额（只输入数字，不显示给用户）
- 添加备注（可选）

### 2. 测试用户页面

访问用户页面：`https://your-domain.com/parent/my-equity`

应该能看到：
- 总股份百分比
- 饼图（投资股份、邀请贡献、人脉贡献）
- 详细明细列表
- 行动指南

### 3. 调整规则（可选）

如果需要修改股权计算规则，在管理后台可以调整：

- 投资股份池占比（默认33.33%）
- 每邀请1人的股份（默认0.05%）
- 每100人脉的股份（默认0.02%）

---

## ⚠️ 注意事项

1. **备份数据库**：迁移前建议先备份生产数据库
2. **测试环境**：如果有测试环境，建议先在测试环境执行
3. **权限检查**：确保数据库用户有 CREATE TABLE 和 INSERT 权限
4. **字符集**：表使用 utf8mb4，确保数据库支持
5. **外键约束**：依赖 users 表，确保该表存在且有 id 字段

---

## 🐛 常见问题

### Q1: 执行时提示 "Table 'users' doesn't exist"

**原因：** 外键约束依赖 users 表  
**解决：** 检查数据库中是否有 users 表，或者修改 FOREIGN KEY 约束

### Q2: 执行时提示 "Duplicate entry for key 'rule_key'"

**原因：** 规则已存在（重复执行）  
**解决：** 这是正常的，SQL 使用了 `ON DUPLICATE KEY UPDATE`，会自动更新现有规则

### Q3: 投资金额显示为0

**原因：** 还没有添加投资记录  
**解决：** 管理员需要在后台手动添加每个股东的投资金额

---

## 📞 技术支持

如果迁移过程中遇到问题，请提供以下信息：

1. 错误信息截图
2. 数据库版本（MySQL / MariaDB / TiDB）
3. 执行的SQL语句
4. 数据库用户权限

---

**迁移文件位置：** `migrations/add_equity_system.sql`  
**相关代码：** `server/db-equity.ts`, `server/equity-router.ts`  
**前端页面：** `client/src/pages/MyEquity.tsx`, `client/src/pages/admin/EquityManagement.tsx`
