# 报销状态管理功能设计方案

## 📋 需求分析

**场景**：公司账本中，有些支出需要报销，需要明确标记哪些已报销、哪些未报销。

**核心需求**：
1. 标记某笔账目是否需要报销
2. 标记报销状态（未报销/已报销）
3. 快速筛选查看未报销的账目
4. 统计未报销金额

---

## 🎯 方案一：简单报销标记（推荐）

### 界面设计

**账目详情页**：
```
┌─────────────────────────────┐
│ 账目详情                     │
├─────────────────────────────┤
│ 金额：¥128.50              │
│ 分类：餐饮                  │
│ 日期：2026-02-13           │
│                            │
│ 报销状态：                  │
│ ○ 无需报销                 │
│ ● 待报销  [标记为已报销]   │ ← 按钮
│ ○ 已报销                   │
└─────────────────────────────┘
```

**账目列表页**：
```
┌─────────────────────────────┐
│ 筛选：[全部] [待报销] [已报销] │
├─────────────────────────────┤
│ 🍜 午餐          -¥45.00    │
│    💰 待报销                │ ← 标签
│                            │
│ 🚕 打车          -¥28.50    │
│    ✅ 已报销                │ ← 标签
└─────────────────────────────┘
```

**统计卡片**（顶部新增）：
```
┌──────────────────┐
│ 💰 待报销金额     │
│    ¥ 256.80      │
│    3笔           │
└──────────────────┘
```

### 数据库设计

**方案1A：添加报销字段**
```sql
ALTER TABLE ledger_records 
ADD COLUMN reimbursement_status ENUM('none', 'pending', 'completed') 
DEFAULT 'none' COMMENT '报销状态：none-无需报销，pending-待报销，completed-已报销';

ADD COLUMN reimbursed_at DATETIME NULL COMMENT '报销时间';

ADD COLUMN reimbursed_by VARCHAR(255) NULL COMMENT '报销操作人';
```

### 功能点

✅ **基础功能**：
- 添加/编辑账目时选择是否需要报销
- 标记已报销（记录时间和操作人）
- 筛选查看：全部/待报销/已报销
- 统计待报销金额

✅ **优点**：
- 实现简单，开发快
- 界面清晰，操作直观
- 满足基本需求

❌ **缺点**：
- 功能相对简单
- 无法处理部分报销的情况

---

## 🎯 方案二：完整报销管理（功能更强）

### 界面设计

**账目详情页**：
```
┌─────────────────────────────┐
│ 账目详情                     │
├─────────────────────────────┤
│ 金额：¥128.50              │
│ 分类：餐饮                  │
│                            │
│ 📋 报销信息                 │
│ ┌─────────────────────────┐ │
│ │ 需要报销：是             │ │
│ │ 报销金额：¥128.50       │ │
│ │ 报销状态：待报销         │ │
│ │ 申请时间：2026-02-13    │ │
│ │                         │ │
│ │ [标记为已报销]          │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

**报销管理页面**（新增）：
```
┌─────────────────────────────┐
│ 报销管理                     │
├─────────────────────────────┤
│ 待报销：3笔  ¥256.80        │
│ 已报销：12笔 ¥1,234.50      │
├─────────────────────────────┤
│ [待报销] [已报销] [全部]    │
├─────────────────────────────┤
│ ☑ 午餐      ¥45.00  2/10    │
│ ☑ 打车      ¥28.50  2/11    │
│ ☑ 文具      ¥183.30 2/12    │
│                            │
│ [批量标记为已报销]          │
└─────────────────────────────┘
```

### 数据库设计

**方案2A：新建报销表**
```sql
CREATE TABLE reimbursements (
  id VARCHAR(255) PRIMARY KEY,
  record_id VARCHAR(255) NOT NULL COMMENT '账目ID',
  ledger_id VARCHAR(255) NOT NULL COMMENT '账本ID',
  amount DECIMAL(10,2) NOT NULL COMMENT '报销金额',
  status ENUM('pending', 'approved', 'rejected', 'completed') DEFAULT 'pending',
  applied_at DATETIME NOT NULL COMMENT '申请时间',
  applied_by VARCHAR(255) NOT NULL COMMENT '申请人',
  completed_at DATETIME NULL COMMENT '完成时间',
  completed_by VARCHAR(255) NULL COMMENT '完成人',
  notes TEXT NULL COMMENT '备注',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (record_id) REFERENCES ledger_records(id),
  FOREIGN KEY (ledger_id) REFERENCES ledgers(id)
);
```

### 功能点

✅ **完整功能**：
- 报销申请（可以部分金额报销）
- 报销审批流程（待审批/已批准/已拒绝/已完成）
- 批量操作（批量标记已报销）
- 报销历史记录
- 报销统计报表
- 导出报销清单

✅ **优点**：
- 功能完整，专业
- 支持复杂场景（部分报销、审批流程）
- 数据独立，便于管理

❌ **缺点**：
- 开发工作量大
- 界面复杂度增加
- 可能过度设计（如果只是简单标记）

---

## 🎯 方案三：混合方案（平衡方案）

### 设计思路

**第一阶段**：实现方案一（简单标记）
- 快速上线，满足基本需求
- 数据库预留扩展字段

**第二阶段**：根据使用情况决定是否升级
- 如果需求简单，保持现状
- 如果需要更多功能，升级到方案二

### 数据库设计（预留扩展）

```sql
ALTER TABLE ledger_records 
ADD COLUMN reimbursement_status ENUM('none', 'pending', 'completed') 
DEFAULT 'none';

ADD COLUMN reimbursement_amount DECIMAL(10,2) NULL COMMENT '报销金额（预留，支持部分报销）';

ADD COLUMN reimbursed_at DATETIME NULL;

ADD COLUMN reimbursed_by VARCHAR(255) NULL;

ADD COLUMN reimbursement_notes TEXT NULL COMMENT '报销备注（预留）';
```

---

## 📊 方案对比

| 特性 | 方案一 | 方案二 | 方案三 |
|------|--------|--------|--------|
| 开发时间 | 1-2小时 | 1-2天 | 分阶段 |
| 功能完整度 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 界面复杂度 | 简单 | 复杂 | 适中 |
| 部分报销 | ❌ | ✅ | 可扩展 |
| 审批流程 | ❌ | ✅ | 可扩展 |
| 批量操作 | 可实现 | ✅ | 可实现 |
| 报销报表 | 基础 | 完整 | 基础→完整 |
| 适用场景 | 个人/小团队 | 企业 | 通用 |

---

## 💡 我的推荐

### 推荐：**方案三（混合方案）**

**理由**：
1. **快速上线**：先实现简单标记，立即可用
2. **灵活扩展**：数据库预留字段，后续可升级
3. **成本可控**：避免过度设计，按需开发
4. **用户友好**：界面简洁，学习成本低

**实施步骤**：
1. 第一阶段（本次）：
   - 添加报销状态字段
   - 实现标记功能
   - 添加筛选和统计
   - **开发时间：1-2小时**

2. 第二阶段（按需）：
   - 如果需要审批流程 → 升级到方案二
   - 如果需要部分报销 → 添加金额字段
   - 如果需要报表 → 开发报销管理页面

---

## 🎨 界面预览（方案三第一阶段）

### 1. 添加账目时
```
┌─────────────────────────────┐
│ 添加账目                     │
├─────────────────────────────┤
│ 金额：¥ 128.50             │
│ 分类：餐饮                  │
│ 日期：2026-02-13           │
│                            │
│ ☑ 需要报销                 │ ← 新增复选框
└─────────────────────────────┘
```

### 2. 账目列表
```
┌─────────────────────────────┐
│ 筛选：[全部▼]               │
│   ├ 全部                   │
│   ├ 待报销 (3)             │
│   └ 已报销 (12)            │
├─────────────────────────────┤
│ 💰 待报销：¥256.80 (3笔)   │ ← 新增统计卡片
├─────────────────────────────┤
│ 🍜 午餐          -¥45.00    │
│    💰 待报销   2月10日      │
│                            │
│ 🚕 打车          -¥28.50    │
│    ✅ 已报销   2月11日      │
└─────────────────────────────┘
```

### 3. 账目详情
```
┌─────────────────────────────┐
│ 账目详情                     │
├─────────────────────────────┤
│ 金额：¥128.50              │
│ 分类：餐饮                  │
│ 日期：2026-02-13           │
│                            │
│ 报销状态：                  │
│ ┌─────────────────────────┐ │
│ │ 💰 待报销               │ │
│ │                         │ │
│ │ [✓ 标记为已报销]        │ │ ← 按钮
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

### 4. 已报销状态
```
┌─────────────────────────────┐
│ 报销状态：                  │
│ ┌─────────────────────────┐ │
│ │ ✅ 已报销               │ │
│ │ 报销时间：2026-02-13    │ │
│ │ 操作人：jiang           │ │
│ │                         │ │
│ │ [取消报销]              │ │ ← 可撤销
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

---

## 🚀 技术实现要点

### 数据库迁移
```sql
-- 添加报销相关字段
ALTER TABLE ledger_records 
ADD COLUMN reimbursement_status ENUM('none', 'pending', 'completed') DEFAULT 'none',
ADD COLUMN reimbursed_at DATETIME NULL,
ADD COLUMN reimbursed_by VARCHAR(255) NULL;
```

### API 新增接口
```typescript
// 标记报销状态
markReimbursement: {
  input: { recordId: string, status: 'pending' | 'completed' },
  output: { success: boolean }
}

// 获取报销统计
getReimbursementStats: {
  input: { ledgerId: string },
  output: { pending: number, completed: number, pendingAmount: number }
}

// 筛选报销账目
getTransactionsList: {
  input: { ledgerId: string, reimbursementFilter?: 'all' | 'pending' | 'completed' }
}
```

### 前端组件
- `ReimbursementBadge.tsx` - 报销状态标签
- `ReimbursementFilter.tsx` - 报销筛选器
- `ReimbursementStats.tsx` - 报销统计卡片
- 修改 `LedgerDetail.tsx` - 添加报销标记功能

---

## ❓ 你的选择

请告诉我你想要哪个方案：

1. **方案一**：简单快速，只要基本的标记功能
2. **方案二**：完整专业，需要审批流程和详细管理
3. **方案三**：混合方案，先简单后扩展（推荐）

或者你有其他想法也可以告诉我！😊
