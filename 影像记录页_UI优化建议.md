# 影像记录页 UI / UX 优化建议

> 基于你提供的录屏与截图，以齿科临床真实使用场景为出发点的设计评审。下面先指出现状问题，再给出可落地的优化方案，并按优先级排序，方便你决定做哪些。

## 一、现状概览

现有「影像记录（患者影像）」页结构为：顶部导航栏（返回 / 标题 / `+`）→ 横向分类 Tab（全部、面像照、X光片、口内照、内窥镜、照片、CBCT、口扫stl、小牙片、美白、文档图片、其他，共 12 类）→ 单列影像卡片（时间戳 + 缩略图 + 分类/备注 + 编辑铅笔）。点击缩略图进入全黑大图页（返回 / 删除 / 添加备注），添加备注为独立编辑态。整体是医疗 SaaS 的标准蓝白风格，干净克制。

底子是好的，问题主要集中在「信息层级、操作路径、影像专业性」三个层面。

## 二、核心问题诊断

| 序号 | 问题 | 影响 |
|------|------|------|
| 1 | 缩略图正中央叠加半透明「添加备注」字样 | 遮挡医学影像关键区域，视觉杂乱，且把"备注"这种低频操作放在最显眼处，喧宾夺主 |
| 2 | 12 个分类全部平铺成横向 Tab | 选项过多需反复横滑，找不到目标分类；多数分类常年无数据仍占位 |
| 3 | `+` 上传仅弹出「分类滚轮选择器」 | 缺少"拍照 / 从相册选 / 选文件"的明确动作指引，用户不知道下一步干什么 |
| 4 | 单列大缩略图列表 | 一屏只能看 1–2 条，影像多时浏览效率低，无法快速比对 |
| 5 | 大图页操作分散 | 删除在右上、备注在右下，焦点割裂；进入备注时图片被键盘大幅压缩，体验生硬 |
| 6 | 无多选 / 批量操作 | 无法批量删除、批量导出、批量归类 |
| 7 | 缺少关键临床能力 | 无法两张影像左右对比、无画笔标注、无治疗阶段（术前/术中/术后）维度 |
| 8 | 时间显示为未来日期 2026-06-14 | 疑似日期校验漏洞或测试脏数据，显得不严谨 |

## 三、优化方案（按模块）

### 1. 列表页：从"单列大图"改为"网格相册 + 时间分组"

齿科影像本质是"病例相册"，应贴近用户熟悉的系统相册心智：

- **缩略图改为 3 列方形网格**，一屏可见 9–12 张，浏览与比对效率成倍提升。
- **按拍摄日期分组**，每组顶部显示日期（如「2025-06-14 · 3 张」），符合"按就诊次回看"的习惯。
- **移除缩略图上的"添加备注"浮层**：缩略图保持纯净，只在右上角用一个极小的角标表示"已有备注"（如一个小圆点或便签图标），点开大图再编辑。
- **缩略图左下角叠加极小的分类标签**（如"X光"），不挡主体；有备注时下方显示一行省略文字。

### 2. 分类导航：从"12 平铺 Tab"改为"智能精简 + 折叠"

- **Tab 只显示有数据的分类**，空分类不占位；「全部」恒在最前。
- 分类较多时，Tab 末尾放一个「更多 ∨」收起其余分类到下拉面板。
- 每个 Tab 后带数量角标（如「X光片 3」），未看先知有多少。

### 3. 上传：把"+"变成清晰的动作菜单

点击 `+` 后，底部弹出的应是**动作优先**的菜单，而非先选分类：

- 第一层动作：**拍照上传 / 从相册选择 / 选择文件（CBCT、stl、文档）**，每项配 lucide 图标。
- 选好图后**再选分类**（默认智能推荐，可改），支持一次多选多张、统一归类。
- 上传时显示进度与缩略预览，支持失败重传。

### 4. 大图查看：聚焦影像本身 + 临床级能力

- **顶部统一操作区**：返回（左），右侧用一个"更多 ⋯"菜单收纳删除/下载/分享/设为封面，避免删除按钮孤零零地高危暴露。
- **备注改为底部可收起的浮层**：默认半透明一行显示，点击展开编辑，不再让图片被键盘挤压变形。
- **手势支持**：双指缩放、双击放大、左右滑切换同分类下一张（看片必备）。
- **新增「对比模式」**：选两张影像左右/上下并排，用于术前术后对照，这是齿科沟通与病例汇报的高频刚需。
- **新增「画笔标注」**：可在影像上圈点、画箭头、加文字，医生向顾客讲解或留存沟通记录极其有用（标注另存为图层，不破坏原图）。

### 5. 治疗阶段维度（强烈建议）

在"分类"之外，增加一个**「治疗阶段」标签**：术前 / 术中 / 术后 / 复查。临床回看时常按阶段而非按影像类型查找，这个维度比 12 个细分类更贴合实际工作流。可与分类组合筛选。

### 6. 批量管理

- 列表页长按或点「管理」进入多选：批量删除、批量改分类/阶段、批量下载。
- 与你顾客列表页刚做的批量管理交互保持一致，降低学习成本。

### 7. 细节与一致性

- **修复未来日期**：排查时间写入逻辑，默认取当前时间，并对手填日期做校验。
- **空状态升级**：除插画外，直接放一个醒目的「上传第一张影像」按钮，减少一次寻找。
- **加载优化**：缩略图用低清占位 + 渐进加载；大图"下载中"提示改为带进度条，避免纯文字干等。
- **风格统一**：图标统一 lucide 线性风格，与牙伴其余页面一致（当前 3D 拟物图标偏重，可保留入口图标但页内操作图标走线性）。

## 四、优先级建议

| 优先级 | 项目 | 理由 |
|--------|------|------|
| P0 速赢 | 移除缩略图"添加备注"浮层；改 3 列网格 + 日期分组；空分类不显示 + 数量角标；修复未来日期 | 改动不大、立竿见影，直接解决"乱、挤、不专业" |
| P1 核心 | 上传动作菜单化（拍照/相册/文件）；大图手势缩放 + 左右滑切换；备注浮层化；治疗阶段维度 | 显著提升录入与看片效率，贴合临床动线 |
| P2 进阶 | 对比模式；画笔标注；批量管理；分享/导出 | 临床沟通与病例管理的高阶能力，提升专业度与差异化 |

## 五、一句话总结

把这个页面从"一个能传图的列表"升级为"一本好用的临床影像病例册"：**列表更像相册（网格+分组）、看图更专业（缩放/对比/标注）、录入更顺手（动作菜单+阶段标签）**。先做 P0 速赢解决观感，再按 P1→P2 逐步补齐临床能力。
